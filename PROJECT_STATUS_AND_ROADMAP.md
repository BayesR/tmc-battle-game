# TMC Battle Game（NPC対戦モード）— 現状まとめ・今後の実装予定・デザイナー視点レポート

作成日：Phase 1 プロトタイプ完成時点

---

## 1. 現状のゲームシステム（完結まとめ）

### 1.1 基本構成
- **技術**：React + TypeScript + Tailwind CSS + Vite
- **データ**：`TMC_NPC_CardPool.xlsx`（238種類）をJSON化して使用
- **判定ロジック**：既存TMC Battle Simulatorの `compareCards`（Root Counter→PP→Void→比較→引分）をそのまま移植
- **デッキルール**：聖・邪・Void合計3枚まで（Void1枚）／Monster Pride合計15以下

### 1.2 画面構成
| 画面 | 内容 |
|---|---|
| デッキ構築 | カードプールから5枚選択。名前・Legacy・MP・PP・Void有無で絞り込み検索可。並び順は「同Legacyでグループ化→MP高い順→PP合計高い順」。NPCレベル選択とデッキプレビューあり |
| 対戦 | JANOでカード選択→盤面に裏向き表示→タップでPull up→表向き公開・WIN/LOSE/DRAWスタンプ→NEXTで負けたカードのみ裏返り次ラウンドへ。5戦後、同数ならサドンデス |
| 結果 | 勝敗表示、対戦の記録一覧、再戦／デッキ組み直しの選択 |

### 1.3 カードビジュアル
- 縦長・角丸、Legacyカラーで着色。左上=モンスター名／右上=Legacy／右端中央=Void「V」／左下=PP（Legacy+数値）／右下=MP（数字）
- 邪(Darkness)は視認性のため枠線・文字色を明るく調整
- 裏面：「TMC」ロゴ＋縁飾り

### 1.4 対戦盤面
- ブラウン基調のマット。使用済みカードは盤面に残り続け、サドンデス突入時にクリア
- 横スクロールは自分・相手が常に連動、ラウンド進行に合わせて最新の対戦へ自動追従
- 相手キャラクターは右上の窓枠アイコンにバストアップ表示

### 1.5 NPCレベル別プロファイル（テスト実装済み）
| レベル | デッキ構築ルール | 対戦時の出し方 | ビジュアル |
|---|---|---|---|
| Lv1 | 聖・邪不使用／MP12〜13／MP5・Void不使用 | 完全ランダム | フードの人 |
| Lv2 | 邪不使用／MP14〜15／MP5とVoid必須 | 完全ランダム | 甲冑の騎士 |
| Lv3 | 邪不使用／MP15固定／MP5・MP1・Void必須／PP優先 | 初手は特殊カードからランダム、以降ランダム | ドラゴン顔ヒューマノイド |
| Lv4 | 聖・邪を1枚ずつ以上／MP15固定／MP5か6・MP1・Void必須／PP優先 | 初手は同様、以降はプレイヤーの傾向を予測して対応 | 死神（骸骨と鎌） |

実データで200回ずつ生成テスト済み・全レベル100%成功。

### 1.6 未検証・未実施の項目
- 実機以外のブラウザ（Safari/Firefox等）での横断確認
- 本番デプロイ（GitHub＋Vercel、現在保留中）
- 負荷・パフォーマンス（カード枚数が増えた場合の描画速度等）

---

## 2. 今後の実装予定（ロードマップ）

### Phase 1 仕上げ（着手しやすい順）
1. **デッキの保存**：`localStorage`に直近の自分デッキを保存し、次回起動時に復元
2. **横向き画面対応**：CSSの`@media (orientation: landscape)`でレイアウト最適化
3. **チュートリアル／初回説明**：JANO→Pull up→NEXTの流れを初回のみ軽く案内
4. **本番デプロイ**：GitHub＋Vercel（ユーザーの準備が整い次第）

### Phase 2：性格モード／カード解放システム
- 性格モード（ルーズ／クレバー／アグレッシブ）→ `pickEnemyCard()`を差し替えるだけの設計に既にしてある
- カード解放システム：勝利数を記録し、使用可能カードプールを段階解放
  - 設計判断が必要：通算勝利か連続勝利か、解放対象カードの決め方

### Phase 3：ストーリーモード
- NPCごとの固定デッキ・テーマLegacy（`generateNpcDeck`をNPC別固定関数に差し替え）
- NPCの見た目個別差し替え（`OpponentPortrait`は既に差し替え可能な設計）
- 撃破によるカードプール解放（Phase 2の解放システムと連動）

### Phase 4：演出強化
- Lv2（Framer Motion）：カードめくり演出、WIN/LOSEのパルス・シェイク
- Lv3（Lottie等）：必殺技風演出、SE連動（サウンド素材の準備が必要）

---

## 3. ゲームデザイナー視点レポート：追加すると面白くなりそうな要素

現状のPhase 1は「対戦の骨組み」として完成度が高い一方、**プレイを繰り返したくなる動機付け（リプレイ性）**と**対戦中の駆け引きの深み**という2軸で伸びしろがあります。優先度と合わせて提案します。

### 🔥 優先度高：リプレイ性を生む仕組み
1. **連勝記録・実績バッジ**
   「Lv3に3連勝」「初手Root Counter発動」等の達成条件でバッジ解放。カード解放システムと同じ基盤（localStorage）で低コスト実装可能。
2. **NPCの決め台詞・煽り**
   対戦開始時／勝利時／敗北時にNPCごとの短い一言を表示（例：Lv4死神「終わりの時だ」）。テキストのみなので低コストだが没入感が大きく上がる。
3. **デッキプリセット保存（複数スロット）**
   「攻撃特化」「バランス型」など複数デッキを保存・切り替え。周回プレイでの試行錯誤を促す。

### ⚔️ 優先度中：対戦の駆け引きを深める
4. **コンボ／シナジー要素**
   同じLegacyを連続で出すと小さなボーナス、あるいは特定のLegacy順で出すと演出が変わる、など。現状はランダム出しなので「読み合い」要素が薄い。プレイヤーの手札に「出す順番」の戦略性を持たせられる。
5. **緊張感を煽るUI**
   Pull up前に一瞬「溜め」の演出（カードが小刻みに震える等）を入れると、公開の瞬間の高揚感が増す。Lv2の演出強化と相性が良い。
6. **サドンデスの専用演出**
   現状は通常戦と見た目がほぼ同じ。盤面の色を変える、BGMを切り替える等で「特別な場面」感を出す。

### 🎲 優先度中：やり込み要素
7. **図鑑（コレクション画面）**
   238種類のカードを「発見済み／未発見」で管理する図鑑UI。カード解放システムと自然に接続できる。
8. **NPCごとの周回チャレンジ**
   「Lv4を無敗で撃破」「Voidカードを使わずに勝利」等のデイリー／ウィークリーチャレンジ。ストーリーモードの前段階として実装しやすい。

### 🌐 優先度低（工数大・将来的検討）
9. **オンライン対人戦**
   NPC対戦の骨組みを流用しやすい設計（`pickEnemyCard`を人間の入力に差し替えるだけ）だが、通信・マッチング基盤が別途必要。
10. **カード育成・強化要素**
    同名カード複数枚で強化する等、TCGでよくある成長要素。ただしBATTLE RULESのバランス設計に影響するため要検討。
11. **シーズンイベント／期間限定NPC**
    ストーリーモード実装後の拡張として。

### 所感
Phase 1で「対戦が正しく機能する」ことを固めたのは正しい優先順位です。次のステップとしては、**大きな新機能を足すより先に、上記①〜③のような低コストな「もう一戦やりたくなる」仕掛け**を先に入れると、Phase 2以降（性格モード・カード解放）の効果も体感しやすくなると考えます。

---

## 4. 次にご相談したいこと

- 上記のうち、どれから着手するか（個人的には①連勝記録＋②NPC決め台詞が「低コスト×高い体感向上」でおすすめです）
- 公開（GitHub＋Vercel）を再開するタイミング
- ストーリーモードのNPC構成（何体、テーマ性）を先に決めておくか
